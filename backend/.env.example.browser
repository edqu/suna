# Browser-First Configuration Example (Option 1)
# Copy relevant lines to your backend/.env file

# ============================================================================
# OPTION 1: Browser Default with API Fallback (Recommended)
# ============================================================================

# Make browser the default for web searches (with API fallback)
BROWSER_DEFAULT_FOR_WEB_SEARCH=true

# REQUIRED: Gemini API key for browser tools
# Get free key: https://aistudio.google.com/app/apikey
# Free tier: 15 requests/min, 1500/day - plenty for development!
GEMINI_API_KEY=your-gemini-api-key-here

# Browser runs in Docker sandbox (headless mode)
STAGEHAND_MODE=managed

# ============================================================================
# HOW IT WORKS
# ============================================================================
# - Browser tools tried FIRST for web tasks
# - API search (DuckDuckGo) used as FALLBACK if browser fails
# - Both tools available - model chooses best for each task
# - Simple one-line toggle to enable/disable

# ============================================================================
# TO DISABLE (Back to API-First)
# ============================================================================
# BROWSER_DEFAULT_FOR_WEB_SEARCH=false
# or just comment out / remove the line

# ============================================================================
# VERIFICATION
# ============================================================================
# After restarting backend, look for:
# ✅ "🌐 Browser-first mode enabled"
# ✅ "✅ Registered LOCAL web search tool"
# ✅ "✅ Registered browser_tool"
